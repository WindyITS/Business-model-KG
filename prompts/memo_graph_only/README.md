# Memo Graph-Only Prompt Assets

This directory contains the editable prompts for the `memo_graph_only` ablation pipeline.

The initial files are exact copies of the analyst pipeline prompts used for:

- the first memo stage
- the graph compilation stage

The analyst augmentation and graph critique prompts are intentionally absent.
